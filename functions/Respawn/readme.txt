This respawn system uses ACE3's save/load gear functions [1] together with
arma3's native onPlayerRespawn.sqf in mission root. Use this to either load
gear saved on death (although ACE3 itself has this functionality) or save the
gear on the first spawn and load after respawn.
